import { type } from "os";

type Token = {
  id: string;
  name: string;
  current_price: number | string;
  symbol: string;
  image: string;
};

// type Name = (tokenName: Token) => void;
// type Price = (tokenPrice: Token) => void;
// type Symbol = (tokenSymbol: Token) => void;
// type Icon = (tokenIcon: Token) => void;
